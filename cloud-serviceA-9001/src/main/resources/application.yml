server:
  port: 9001

spring:
  application:
    name: service-A

eureka:
  client:
    register-with-eureka: true # 向注册中心注册自己
    fetch-registry: true # 拉取注册中心的信息
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka,http://eureka7002.com:7002/eureka
  #instance:
  #  instance-id: serviceA-9001